---
const roomId = Math.random().toString(36).substring(2, 15);
const href = `/room/${roomId}`;
---

<a
  id="new-room"
  class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
  href={href}>Generate New Team Room</a>
